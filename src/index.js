import React from 'react';
import ReactDOM from 'react-dom/client';
import SoilMonitorDashboard from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SoilMonitorDashboard />
  </React.StrictMode>
);